CHANGELOG

Note: I cannot read your local Git history here. To generate a full changelog, run locally:

```powershell
git log --pretty=format:"%h %ad %s" --date=short --no-merges
```

Suggested changelog format (starter):

## Unreleased

### Added
- Initial monorepo with `Client/` (Angular) and `server/` (.NET solution)

### Changed
- Migrated EF model to Database-First (Entities generated by EF Core Power Tools)

### Fixed
- (fill in after reviewing commit history)

### Chore
- Add CI workflow for building and testing both Client and server

How to produce final CHANGELOG
1. Run `git log --oneline --no-merges` locally.
2. Group commits into Added / Changed / Fixed / Chore.
3. Paste grouped commits under the sections above and save `CHANGELOG.md`.

If you paste the output here I will format it into a clean CHANGELOG for you.
